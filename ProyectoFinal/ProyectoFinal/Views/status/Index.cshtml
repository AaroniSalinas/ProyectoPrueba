@model IEnumerable<ProyectoFinal.status>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.statusNombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.statusNombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.statusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.statusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.statusId })
        </td>
    </tr>
}

</table>
