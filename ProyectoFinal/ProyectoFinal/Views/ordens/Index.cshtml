@model IEnumerable<ProyectoFinal.orden>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ordenTotal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status.statusNombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.usuario.usuarioCorreo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ordenTotal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.status.statusNombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.usuario.usuarioCorreo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ordenId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ordenId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ordenId })
        </td>
    </tr>
}

</table>
