@model IEnumerable<ProyectoFinal.categoria>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.categoriaNombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.categoriaNombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.categoriaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.categoriaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.categoriaId })
        </td>
    </tr>
}

</table>
